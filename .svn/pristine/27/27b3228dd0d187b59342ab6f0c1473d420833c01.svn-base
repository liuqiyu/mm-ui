import Vue from 'vue'

const ASPContainer  = Vue.component('asp-container', {
  functional: true,
  render (h, self) {
    return h('el-container', self.data, self.children)
  }
})

export default ASPContainer
