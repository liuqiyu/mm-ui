import Vue from 'vue'

const ASPDialog = Vue.component('asp-dialog', {
  functional: true,
  render (h, self) {
    return h('el-dialog', self.data, self.children)
  }
})

export default ASPDialog
