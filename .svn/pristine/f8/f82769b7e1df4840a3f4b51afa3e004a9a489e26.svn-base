import Vue from 'vue'

const ASPBreadcrumb  = Vue.component('asp-breadcrumb', {
  functional: true,
  render (h, self) {
    return h('el-breadcrumb', self.data, self.children)
  }
})

export default ASPBreadcrumb
