import Vue from 'vue'

const ASPDivider = Vue.component('asp-divider', {
  functional: true,
  render (h, self) {
    return h('el-divider', self.data, self.children)
  }
})

export default ASPDivider
