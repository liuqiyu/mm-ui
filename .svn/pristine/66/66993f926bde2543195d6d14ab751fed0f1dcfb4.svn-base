import Vue from 'vue'

const ASPProgress = Vue.component('asp-progress', {
  functional: true,
  render (h, self) {
    return h('el-progress', self.data, self.children)
  }
})

export default ASPProgress
