import Vue from 'vue'

const ASPTimeline = Vue.component('asp-timeline', {
  functional: true,
  render (h, self) {
    return h('el-timeline', self.data, self.children)
  }
})

export default ASPTimeline
