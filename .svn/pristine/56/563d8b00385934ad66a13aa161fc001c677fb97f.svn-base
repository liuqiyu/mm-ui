import Vue from 'vue'

const ASPCollapse = Vue.component('asp-collapse', {
  functional: true,
  render (h, self) {
    return h('el-collapse', self.data, self.children)
  }
})

export default ASPCollapse
