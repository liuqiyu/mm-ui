import Vue from 'vue'

const ASPIcon  = Vue.component('asp-icon', {
  functional: true,
  render (h, self) {
    return h('el-icon', self.data, self.children)
  }
})

export default ASPIcon
