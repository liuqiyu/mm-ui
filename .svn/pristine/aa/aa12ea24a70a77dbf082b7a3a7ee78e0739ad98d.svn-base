import Vue from 'vue'

const ASPDrawer = Vue.component('asp-drawer', {
  functional: true,
  render (h, self) {
    return h('el-drawer', self.data, self.children)
  }
})

export default ASPDrawer
