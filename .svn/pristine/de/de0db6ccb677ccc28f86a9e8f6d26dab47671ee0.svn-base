import Vue from 'vue'

const ASPAlert = Vue.component('asp-alert', {
  functional: true,
  render (h, self) {
    return h('el-alert', self.data, self.children)
  }
})

export default ASPAlert
