import Vue from 'vue'

const ASPTooltip = Vue.component('asp-tooltip', {
  functional: true,
  render (h, self) {
    return h('el-tooltip', self.data, self.children)
  }
})

export default ASPTooltip
