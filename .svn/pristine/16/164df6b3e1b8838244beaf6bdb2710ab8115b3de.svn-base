import Vue from 'vue'

const ASPTable = Vue.component('asp-table', {
  functional: true,
  render (h, self) {
    return h('el-table', self.data, self.children)
  }
})

export default ASPTable
