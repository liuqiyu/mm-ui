import Vue from 'vue'

const ASPPopover = Vue.component('asp-popover', {
  functional: true,
  render (h, self) {
    return h('el-popover', self.data, self.children)
  }
})

export default ASPPopover
