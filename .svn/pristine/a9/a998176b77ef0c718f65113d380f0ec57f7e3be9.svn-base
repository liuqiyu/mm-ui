import Vue from 'vue'

const ASPRate  = Vue.component('asp-rate', {
  functional: true,
  render (h, self) {
    return h('el-rate', self.data, self.children)
  }
})

export default ASPRate
