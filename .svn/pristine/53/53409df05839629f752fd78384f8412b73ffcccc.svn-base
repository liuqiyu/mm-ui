import Vue from 'vue'

const AspButton = Vue.component('asp-button', {
  functional: true,
  render (h, self) {
    return h('el-button', self.data, self.children)
  }
})

export default AspButton
