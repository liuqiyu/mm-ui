import Vue from 'vue'

const ASPCarousel = Vue.component('asp-carousel', {
  functional: true,
  render (h, self) {
    return h('el-carousel', self.data, self.children)
  }
})

export default ASPCarousel
